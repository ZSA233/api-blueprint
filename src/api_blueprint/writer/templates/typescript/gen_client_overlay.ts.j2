{% autoescape false %}/* eslint-disable */
/* istanbul ignore file */
// Code generated by api-blueprint (TypeScript); DO NOT EDIT.

import type { ApiClientConfig } from "{{ shared_client_api_path }}";
import { {{ writer.transport_class_name }} } from "{{ transport_path }}";
import { {{ client_class }} as Shared{{ client_class }} } from "{{ shared_client_path }}";

{% if raw_ws_method_names %}
type HiddenRawWebSocketMethods =
{%- for method_name in raw_ws_method_names %}
  {{ " |" if not loop.first else "" }} "{{ method_name }}"
{%- endfor %};

export type {{ client_class }} = Omit<Shared{{ client_class }}, HiddenRawWebSocketMethods>;
{% else %}
export type {{ client_class }} = Shared{{ client_class }};
{% endif %}

export function createClient(config: ApiClientConfig = {}): {{ client_class }} {
  return new Shared{{ client_class }}({
    ...config,
    transport: config.transport ?? new {{ writer.transport_class_name }}(config),
  }) as {{ client_class }};
}
{% endautoescape %}
