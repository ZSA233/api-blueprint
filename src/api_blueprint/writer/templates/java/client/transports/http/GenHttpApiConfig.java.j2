package {{ bp.root_package }}.transports.http;

import java.time.Duration;
import java.util.Map;

public record GenHttpApiConfig(
    String baseUrl,
    Map<String, String> defaultHeaders,
    Duration timeout
) {
    public GenHttpApiConfig() {
        this({{ writer.rendered_base_url | safe }}, Map.of(), null);
    }

    public GenHttpApiConfig(String baseUrl) {
        this(baseUrl, Map.of(), null);
    }

    public GenHttpApiConfig(String baseUrl, Map<String, String> defaultHeaders) {
        this(baseUrl, defaultHeaders, null);
    }

    public GenHttpApiConfig {
        baseUrl = baseUrl == null ? "" : baseUrl;
        defaultHeaders = defaultHeaders == null ? Map.of() : Map.copyOf(defaultHeaders);
    }
}
