package {{ writer.provider_package }}

// User-owned extension point for transport-neutral context helpers.
//
// The Context type and provider pipeline are generated in gen_context.go and
// gen_executor.go. RequestContext, ResponseContext, and HandleContext are also
// generated protocol frames because providers and transports depend on their
// fields. Add project-specific helper methods in this file or another non-gen
// file, but do not re-declare generated context types.
