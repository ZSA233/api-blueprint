// Code generated by api-blueprint (Go client); DO NOT EDIT.

package {{ root_package }}

import (
	httptransport "{{ http_import }}"
	runtime "{{ runtime_import }}"
{% for group in groups %}	{{ group.package }} "{{ route_import(module, group) }}"
{% endfor %})

type HTTPConfig = httptransport.HttpConfig

type Client struct {
{% for group in groups %}	{{ group.client_class.removesuffix("Client") }} *{{ group.package }}.Client
{% endfor %}}

func NewHTTP(config HTTPConfig) *Client {
	transport := httptransport.NewClient(config)
	return NewClient(transport)
}

func NewClient(transport runtime.Transport) *Client {
	return &Client{
{% for group in groups %}		{{ group.client_class.removesuffix("Client") }}: {{ group.package }}.NewClient(transport),
{% endfor %}	}
}
