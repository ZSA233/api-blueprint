// Code generated by api-blueprint (Kotlin client); DO NOT EDIT.
package com.example.apiblueprint.api.runtime

val ApiErrorsByID: Map<String, ApiErrorEntry> = mapOf(
    "CommonErr.UNKNOWN" to ApiErrorEntry(
        id = "CommonErr.UNKNOWN",
        group = "CommonErr",
        key = "UNKNOWN",
        code = -1,
        message = "未知错误",
        toast = ApiToastSpec(
            key = "CommonErr.UNKNOWN",
            level = "error",
            default = "未知错误",
        ),
    ),
    "CommonErr.TOKEN_EXPIRE" to ApiErrorEntry(
        id = "CommonErr.TOKEN_EXPIRE",
        group = "CommonErr",
        key = "TOKEN_EXPIRE",
        code = 55555,
        message = "token登录态失效",
        toast = ApiToastSpec(
            key = "auth.token_expire",
            level = "warning",
            default = "登录状态已失效，请重新登录",
        ),
    ),
    "DemoErr.RATE_LIMITED" to ApiErrorEntry(
        id = "DemoErr.RATE_LIMITED",
        group = "DemoErr",
        key = "RATE_LIMITED",
        code = 42901,
        message = "请求过于频繁",
        toast = ApiToastSpec(
            key = "demo.rate_limited",
            level = "warning",
            default = "请求过于频繁，请稍后再试",
        ),
    ),
)

val ApiErrors: Map<String, ApiErrorEntry> = ApiErrorsByID

private val routeApiErrorsByCode: Map<String, Map<ApiErrorCode, ApiErrorEntry>> = mapOf(
    "api.binary.post.packet" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.binary.post.auditpacket" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.demo.get.abc" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.demo.post.testpost" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.demo.put.z1put" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.demo.stream.sweepevents" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.demo.channel.assistantsession" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.demo.post.postdeprecated" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.demo.post.raw" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.demo.post.mapmodel" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.demo.get.errordemo" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
        42901 to ApiErrorsByID.getValue("DemoErr.RATE_LIMITED"),
    ),
    "api.hello.get.abc" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.hello.get.mapenum" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.hello.get.listenum" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.hello.get.string" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.hello.get.uint64" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.hello.get.stringemun" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
    "api.hello.get.helloway" to mapOf(
        -1 to ApiErrorsByID.getValue("CommonErr.UNKNOWN"),
        55555 to ApiErrorsByID.getValue("CommonErr.TOKEN_EXPIRE"),
    ),
)

fun lookupApiErrorById(id: String): ApiErrorEntry? = ApiErrorsByID[id]

fun lookupApiErrorByCode(code: ApiErrorCode, routeId: String? = null): ApiErrorEntry? {
    if (!routeId.isNullOrEmpty()) {
        routeApiErrorsByCode[routeId]?.get(code)?.let { return it }
    }
    return ApiErrorsByID.values.firstOrNull { it.code == code }
}

fun lookupApiError(payload: ApiErrorPayload?, routeId: String? = null): ApiErrorEntry? {
    if (payload == null) {
        return null
    }
    if (payload.id.isNotEmpty()) {
        lookupApiErrorById(payload.id)?.let { return it }
    }
    return lookupApiErrorByCode(payload.code, routeId)
}

fun apiErrorPayload(
    id: String = "",
    group: String = "",
    key: String = "",
    code: ApiErrorCode = 0,
    message: String = "",
    toast: ApiToastPayload = ApiToastPayload(),
    routeId: String? = null,
): ApiErrorPayload {
    val seed = ApiErrorPayload(id = id, group = group, key = key, code = code, message = message, toast = toast)
    val entry = lookupApiError(seed, routeId)
    return ApiErrorPayload(
        id = id.ifEmpty { entry?.id.orEmpty() },
        group = group.ifEmpty { entry?.group.orEmpty() },
        key = key.ifEmpty { entry?.key.orEmpty() },
        code = if (code != 0) code else entry?.code ?: 0,
        message = message.ifEmpty { entry?.message ?: "API error ${if (code != 0) code else entry?.code ?: 0}" },
        toast = ApiToastPayload(
            key = toast.key.ifEmpty { entry?.toast?.key.orEmpty() },
            level = toast.level.ifEmpty { entry?.toast?.level ?: "error" },
            default = toast.default.ifEmpty { entry?.toast?.default.orEmpty() },
            text = toast.text,
        ),
    )
}

object CommonErr {
    const val UNKNOWN: Int = -1
    const val TOKEN_EXPIRE: Int = 55555
}

object DemoErr {
    const val RATE_LIMITED: Int = 42901
}

