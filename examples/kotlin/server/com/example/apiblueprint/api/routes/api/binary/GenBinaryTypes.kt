// Code generated by api-blueprint (Kotlin server); DO NOT EDIT.
package com.example.apiblueprint.api.routes.api.binary

import com.example.apiblueprint.api.runtime.*
import kotlinx.serialization.builtins.*
import kotlinx.serialization.KSerializer
import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable
import kotlinx.serialization.SerializationException
import kotlinx.serialization.descriptors.PrimitiveKind
import kotlinx.serialization.descriptors.PrimitiveSerialDescriptor
import kotlinx.serialization.descriptors.SerialDescriptor
import kotlinx.serialization.encoding.Decoder
import kotlinx.serialization.encoding.Encoder

import com.example.apiblueprint.api.runtime.binary.ApiBinaryBody
import com.example.apiblueprint.api.runtime.binary.BinaryDecodeException
import com.example.apiblueprint.api.runtime.binary.BinaryEncodeException
import com.example.apiblueprint.api.runtime.binary.BinaryEndian
import com.example.apiblueprint.api.runtime.binary.BinaryReader
import com.example.apiblueprint.api.runtime.binary.BinaryWriter
import com.example.apiblueprint.api.runtime.binary.EncodedBinaryBlock
import com.example.apiblueprint.api.runtime.binary.StreamingBinaryBody
import com.example.apiblueprint.api.runtime.binary.binarySize
import com.example.apiblueprint.api.runtime.binary.requireBinary
import com.example.apiblueprint.api.runtime.binary.requireRange
import com.example.apiblueprint.api.runtime.binary.requireSize
import com.example.apiblueprint.api.runtime.binary.wrapBinaryField
import com.example.apiblueprint.api.runtime.binary.wrapBinaryIndex
import okio.ByteString

@Serializable
public data class BinaryAuditPacketQuery(
    public val trace: String? = null
)

@Serializable
public data class BinaryAuditPacketResponse(
    public val trace: String,
    @SerialName("item_count")
    public val itemCount: Int,
    public val checksum: Int
)

@Serializable
public data class BinaryPacketQuery(
    public val trace: String? = null
)

@Serializable
public data class BinaryPacketResponse(
    public val trace: String,
    public val version: Int,
    @SerialName("item_count")
    public val itemCount: Int,
    public val payload: String,
    @SerialName("score_sum")
    public val scoreSum: Double,
    @SerialName("first_label")
    public val firstLabel: String,
    @SerialName("item_ids")
    public val itemIds: List<Int>,
    public val checksum: Int
)

@Serializable
public data class BinaryWidePacketQuery(
    public val trace: String? = null
)

@Serializable
public data class BinaryWidePacketResponse(
    public val trace: String,
    @SerialName("payload_size")
    public val payloadSize: Long,
    @SerialName("signed_wide")
    public val signedWide: Long,
    public val checksum: Long
)

public object DemoPacketKindValues {
    public const val Metric: Int = 1
    public const val Debug: Int = 2
}

public object DemoPacketFlagsValues {
    public const val HasPayload: Long = 1L
    public const val HasScores: Long = 2L
    public const val FastPath: Long = 4L
    public const val Mode: Long = 24L
    public const val Reserved: Long = 4294967264L
}

public data class DemoPacket(
    public val header: DemoPacketHeader,
    public val body: DemoPacketBody
)

public fun DemoPacket.toBinaryBody(): ApiBinaryBody = DemoPacketWire.toBinaryBody(this)

public class DemoPacketBinaryState {
    public var version: Long = 0L
    public var kind: Long = 0L
    public var flags: Long = 0L
    public var shortCode: Long = 0L
    public var signedDelta: Long = 0L
    public var itemCount: Long = 0L
    public var payloadLen: Long = 0L
    public var scoreCount: Long = 0L
    public var checksum: Long = 0L
    public var id: Long = 0L
    public var labelLen: Long = 0L
}

public data class DemoPacketHeader(
    public val flags: Long,
    public val shortCode: Int,
    public val signedDelta: Int,
    public val itemCount: Int,
    public val payloadLen: Long,
    public val magic: ByteArray = byteArrayOf(65, 66, 80, 49),
    public val version: Int = 1,
    public val kind: Int = 1,
    public val scoreCount: Int = 2
)

public data class DemoPacketBody(
    public val items: List<DemoPacketItem>,
    public val payload: ByteArray,
    public val scores: List<Double>,
    public val checksum: Long
)

public data class DemoPacketItem(
    public val id: Long,
    public val enabled: Boolean,
    public val value: Double,
    public val labelLen: Int,
    public val label: ByteArray
)

public object DemoPacketWire {
    public const val CONTENT_TYPE: String = "application/octet-stream"
    public val ENDIAN: BinaryEndian = BinaryEndian.LITTLE

    public fun body(
        contentLength: Long? = null,
        write: (BinaryWriter) -> Unit,
    ): ApiBinaryBody = StreamingBinaryBody(
        contentType = CONTENT_TYPE,
        contentLength = contentLength,
        endian = ENDIAN,
        write = write,
    )

    public fun toBinaryBody(value: DemoPacket): ApiBinaryBody = body { writer ->
        write(value, writer)
    }

    public fun parse(bytes: ByteArray): DemoPacket {
        val reader = BinaryReader(bytes, ENDIAN)
        val state = DemoPacketBinaryState()
        val header = readDemoPacketHeader(reader, state, "DemoPacket.header")
        val body = readDemoPacketBody(reader, state, "DemoPacket.body")
        reader.requireEOF("DemoPacket")
        return DemoPacket(
            header = header,
            body = body
        )
    }

    public fun write(value: DemoPacket, writer: BinaryWriter) {
        val state = DemoPacketBinaryState()
        writeDemoPacketHeader(value.header, writer, state, "DemoPacket.header")
        writeDemoPacketBody(value.body, writer, state, "DemoPacket.body")
    }

    public fun readDemoPacketHeader(
        reader: BinaryReader,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "DemoPacketHeader",
    ): DemoPacketHeader {
        try {
            val magic = reader.readBytes("magic", 4L)
            requireBinary("magic", magic.contentEquals(byteArrayOf(65, 66, 80, 49)), "const mismatch")
            val version = reader.readU16("version")
            requireBinary("version", version.toLong() == 1L, "const mismatch")
            state.version = version.toLong()
            val kind = reader.readU16("kind")
            requireBinary("kind", kind.toLong() == 1L, "const mismatch")
            state.kind = kind.toLong()
            val flags = reader.readU32("flags")
            requireRange("flags", flags, 0L, Long.MAX_VALUE)
            requireBinary("flags", ((flags) and 4294967264L) == 0L, "reserved bits must be zero")
            state.flags = flags
            reader.readZeroes("header_pad", 1L)
            reader.readZeroes("reserved0", 2L)
            val shortCode = reader.readU24("short_code")
            requireRange("short_code", shortCode.toLong(), 1L, Long.MAX_VALUE)
            requireRange("short_code", shortCode.toLong(), Long.MIN_VALUE, 16777215L)
            state.shortCode = shortCode.toLong()
            val signedDelta = reader.readI24("signed_delta")
            requireRange("signed_delta", signedDelta.toLong(), 0L, Long.MAX_VALUE)
            requireRange("signed_delta", signedDelta.toLong(), Long.MIN_VALUE, 8388607L)
            state.signedDelta = signedDelta.toLong()
            val itemCount = reader.readU16("item_count")
            requireRange("item_count", itemCount.toLong(), 1L, Long.MAX_VALUE)
            requireRange("item_count", itemCount.toLong(), Long.MIN_VALUE, 8L)
            state.itemCount = itemCount.toLong()
            val payloadLen = reader.readU32("payload_len")
            requireRange("payload_len", payloadLen, 0L, Long.MAX_VALUE)
            requireRange("payload_len", payloadLen, Long.MIN_VALUE, 64L)
            state.payloadLen = payloadLen
            val scoreCount = reader.readU16("score_count")
            requireBinary("score_count", scoreCount.toLong() == 2L, "const mismatch")
            requireRange("score_count", scoreCount.toLong(), Long.MIN_VALUE, 4L)
            state.scoreCount = scoreCount.toLong()
            return DemoPacketHeader(
                flags = flags,
                shortCode = shortCode,
                signedDelta = signedDelta,
                itemCount = itemCount,
                payloadLen = payloadLen,
                magic = magic,
                version = version,
                kind = kind,
                scoreCount = scoreCount
            )
        } catch (error: BinaryDecodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeDemoPacketHeader(
        value: DemoPacketHeader,
        writer: BinaryWriter,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "DemoPacketHeader",
    ) {
        try {
            requireBinary("magic", value.magic.contentEquals(byteArrayOf(65, 66, 80, 49)), "const mismatch")
            writer.writeBytesExact("magic", value.magic, 4L)
            requireBinary("version", value.version.toLong() == 1L, "const mismatch")
            writer.writeU16("version", value.version)
            state.version = value.version.toLong()
            requireBinary("kind", value.kind.toLong() == 1L, "const mismatch")
            writer.writeU16("kind", value.kind)
            state.kind = value.kind.toLong()
            requireRange("flags", value.flags, 0L, Long.MAX_VALUE)
            requireBinary("flags", ((value.flags) and 4294967264L) == 0L, "reserved bits must be zero")
            writer.writeU32("flags", value.flags)
            state.flags = value.flags
            writer.writeZeroes("header_pad", 1L)
            writer.writeZeroes("reserved0", 2L)
            requireRange("short_code", value.shortCode.toLong(), 1L, Long.MAX_VALUE)
            requireRange("short_code", value.shortCode.toLong(), Long.MIN_VALUE, 16777215L)
            writer.writeU24("short_code", value.shortCode)
            state.shortCode = value.shortCode.toLong()
            requireRange("signed_delta", value.signedDelta.toLong(), 0L, Long.MAX_VALUE)
            requireRange("signed_delta", value.signedDelta.toLong(), Long.MIN_VALUE, 8388607L)
            writer.writeI24("signed_delta", value.signedDelta)
            state.signedDelta = value.signedDelta.toLong()
            requireRange("item_count", value.itemCount.toLong(), 1L, Long.MAX_VALUE)
            requireRange("item_count", value.itemCount.toLong(), Long.MIN_VALUE, 8L)
            writer.writeU16("item_count", value.itemCount)
            state.itemCount = value.itemCount.toLong()
            requireRange("payload_len", value.payloadLen, 0L, Long.MAX_VALUE)
            requireRange("payload_len", value.payloadLen, Long.MIN_VALUE, 64L)
            writer.writeU32("payload_len", value.payloadLen)
            state.payloadLen = value.payloadLen
            requireBinary("score_count", value.scoreCount.toLong() == 2L, "const mismatch")
            requireRange("score_count", value.scoreCount.toLong(), Long.MIN_VALUE, 4L)
            writer.writeU16("score_count", value.scoreCount)
            state.scoreCount = value.scoreCount.toLong()
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeDemoPacketHeader(
        writer: BinaryWriter,
        flags: Long,
        shortCode: Int,
        signedDelta: Int,
        itemCount: Int,
        payloadLen: Long,
        magic: ByteArray = byteArrayOf(65, 66, 80, 49),
        version: Int = 1,
        kind: Int = 1,
        scoreCount: Int = 2,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "DemoPacketHeader",
    ) {
        try {
            requireBinary("magic", magic.contentEquals(byteArrayOf(65, 66, 80, 49)), "const mismatch")
            writer.writeBytesExact("magic", magic, 4L)
            requireBinary("version", version.toLong() == 1L, "const mismatch")
            writer.writeU16("version", version)
            state.version = version.toLong()
            requireBinary("kind", kind.toLong() == 1L, "const mismatch")
            writer.writeU16("kind", kind)
            state.kind = kind.toLong()
            requireRange("flags", flags, 0L, Long.MAX_VALUE)
            requireBinary("flags", ((flags) and 4294967264L) == 0L, "reserved bits must be zero")
            writer.writeU32("flags", flags)
            state.flags = flags
            writer.writeZeroes("header_pad", 1L)
            writer.writeZeroes("reserved0", 2L)
            requireRange("short_code", shortCode.toLong(), 1L, Long.MAX_VALUE)
            requireRange("short_code", shortCode.toLong(), Long.MIN_VALUE, 16777215L)
            writer.writeU24("short_code", shortCode)
            state.shortCode = shortCode.toLong()
            requireRange("signed_delta", signedDelta.toLong(), 0L, Long.MAX_VALUE)
            requireRange("signed_delta", signedDelta.toLong(), Long.MIN_VALUE, 8388607L)
            writer.writeI24("signed_delta", signedDelta)
            state.signedDelta = signedDelta.toLong()
            requireRange("item_count", itemCount.toLong(), 1L, Long.MAX_VALUE)
            requireRange("item_count", itemCount.toLong(), Long.MIN_VALUE, 8L)
            writer.writeU16("item_count", itemCount)
            state.itemCount = itemCount.toLong()
            requireRange("payload_len", payloadLen, 0L, Long.MAX_VALUE)
            requireRange("payload_len", payloadLen, Long.MIN_VALUE, 64L)
            writer.writeU32("payload_len", payloadLen)
            state.payloadLen = payloadLen
            requireBinary("score_count", scoreCount.toLong() == 2L, "const mismatch")
            requireRange("score_count", scoreCount.toLong(), Long.MIN_VALUE, 4L)
            writer.writeU16("score_count", scoreCount)
            state.scoreCount = scoreCount.toLong()
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun newDemoPacketHeaderReserved0Block(
        bytes: ByteString,
        count: Long,
    ): EncodedBinaryBlock = EncodedBinaryBlock(
        field = "DemoPacketHeader.reserved0",
        count = count,
        bytes = bytes,
    )

    public fun writeDemoPacketHeaderReserved0Block(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "DemoPacketHeader.reserved0",
    ) {
        val reserved0Count = 2L
        requireBinary(path, block.field == "DemoPacketHeader.reserved0", "block field ${block.field} does not match " + "DemoPacketHeader.reserved0")
        requireSize("$path.count", block.count, reserved0Count)

        writer.writeBlock(path, block)
    }

    public fun writeDemoPacketHeaderReserved0Fragment(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "DemoPacketHeader.reserved0",
    ) {
        val reserved0Count = 2L
        requireBinary(path, block.field == "DemoPacketHeader.reserved0", "block field ${block.field} does not match " + "DemoPacketHeader.reserved0")
        requireRange("$path.count", block.count, 0L, reserved0Count)

        writer.writeBlock(path, block)
    }

    public fun readDemoPacketBody(
        reader: BinaryReader,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "DemoPacketBody",
    ): DemoPacketBody {
        try {
            val itemsCount = state.itemCount
            val items = mutableListOf<DemoPacketItem>()
            repeat(itemsCount.toInt()) { index ->
                try {
                    items += readDemoPacketItem(reader, state, "")
                } catch (error: BinaryDecodeException) {
                    throw wrapBinaryIndex("items", index, error)
                }
            }
            val payload = reader.readBytes("payload", state.payloadLen)
            val scoresCount = state.scoreCount
            val scores = mutableListOf<Double>()
            repeat(scoresCount.toInt()) { index ->
                scores += reader.readF64("scores")
            }
            val checksum = reader.readU32("checksum")
            requireBinary("checksum", checksum == state.itemCount + state.payloadLen, "assert mismatch")
            state.checksum = checksum
            return DemoPacketBody(
                items = items,
                payload = payload,
                scores = scores,
                checksum = checksum
            )
        } catch (error: BinaryDecodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeDemoPacketBody(
        value: DemoPacketBody,
        writer: BinaryWriter,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "DemoPacketBody",
    ) {
        try {
            val itemsCount = state.itemCount
            requireSize("items", binarySize(value.items), itemsCount)
            value.items.forEachIndexed { index, item ->
                try {
                    writeDemoPacketItem(item, writer, state, "")
                } catch (error: BinaryEncodeException) {
                    throw wrapBinaryIndex("items", index, error)
                }
            }
            val payloadCount = state.payloadLen
            requireSize("payload", binarySize(value.payload), payloadCount)
            writer.writeBytes("payload", value.payload)
            val scoresCount = state.scoreCount
            requireSize("scores", binarySize(value.scores), scoresCount)
            value.scores.forEachIndexed { index, item ->
                try {
                    writer.writeF64("", item)
                } catch (error: BinaryEncodeException) {
                    throw wrapBinaryIndex("scores", index, error)
                }
            }
            requireBinary("checksum", value.checksum == state.itemCount + state.payloadLen, "assert mismatch")
            writer.writeU32("checksum", value.checksum)
            state.checksum = value.checksum
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun newDemoPacketBodyItemsBlock(
        bytes: ByteString,
        count: Long,
    ): EncodedBinaryBlock = EncodedBinaryBlock(
        field = "DemoPacketBody.items",
        count = count,
        bytes = bytes,
    )

    public fun writeDemoPacketBodyItemsBlock(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "DemoPacketBody.items",
    ) {
        val itemsCount = state.itemCount
        requireBinary(path, block.field == "DemoPacketBody.items", "block field ${block.field} does not match " + "DemoPacketBody.items")
        requireSize("$path.count", block.count, itemsCount)

        writer.writeBlock(path, block)
    }

    public fun writeDemoPacketBodyItemsFragment(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "DemoPacketBody.items",
    ) {
        val itemsCount = state.itemCount
        requireBinary(path, block.field == "DemoPacketBody.items", "block field ${block.field} does not match " + "DemoPacketBody.items")
        requireRange("$path.count", block.count, 0L, itemsCount)

        writer.writeBlock(path, block)
    }

    public fun newDemoPacketBodyPayloadBlock(
        bytes: ByteString,
        count: Long,
    ): EncodedBinaryBlock = EncodedBinaryBlock(
        field = "DemoPacketBody.payload",
        count = count,
        bytes = bytes,
    )

    public fun writeDemoPacketBodyPayloadBlock(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "DemoPacketBody.payload",
    ) {
        val payloadCount = state.payloadLen
        requireBinary(path, block.field == "DemoPacketBody.payload", "block field ${block.field} does not match " + "DemoPacketBody.payload")
        requireSize("$path.count", block.count, payloadCount)

        requireSize("$path.bytes", block.byteSize, block.count)

        writer.writeBlock(path, block)
    }

    public fun writeDemoPacketBodyPayloadFragment(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "DemoPacketBody.payload",
    ) {
        val payloadCount = state.payloadLen
        requireBinary(path, block.field == "DemoPacketBody.payload", "block field ${block.field} does not match " + "DemoPacketBody.payload")
        requireRange("$path.count", block.count, 0L, payloadCount)

        requireSize("$path.bytes", block.byteSize, block.count)

        writer.writeBlock(path, block)
    }

    public fun newDemoPacketBodyScoresBlock(
        bytes: ByteString,
        count: Long,
    ): EncodedBinaryBlock = EncodedBinaryBlock(
        field = "DemoPacketBody.scores",
        count = count,
        bytes = bytes,
    )

    public fun writeDemoPacketBodyScoresBlock(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "DemoPacketBody.scores",
    ) {
        val scoresCount = state.scoreCount
        requireBinary(path, block.field == "DemoPacketBody.scores", "block field ${block.field} does not match " + "DemoPacketBody.scores")
        requireSize("$path.count", block.count, scoresCount)

        requireSize("$path.bytes", block.byteSize, block.count * 8L)

        writer.writeBlock(path, block)
    }

    public fun writeDemoPacketBodyScoresFragment(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "DemoPacketBody.scores",
    ) {
        val scoresCount = state.scoreCount
        requireBinary(path, block.field == "DemoPacketBody.scores", "block field ${block.field} does not match " + "DemoPacketBody.scores")
        requireRange("$path.count", block.count, 0L, scoresCount)

        requireSize("$path.bytes", block.byteSize, block.count * 8L)

        writer.writeBlock(path, block)
    }

    public fun readDemoPacketItem(
        reader: BinaryReader,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "Item",
    ): DemoPacketItem {
        try {
            val id = reader.readU32("id")
            requireRange("id", id, 1L, Long.MAX_VALUE)
            requireRange("id", id, Long.MIN_VALUE, 999L)
            state.id = id
            val enabled = reader.readBool("enabled")
            val value = reader.readF64("value")
            val labelLen = reader.readU8("label_len")
            requireRange("label_len", labelLen.toLong(), 1L, Long.MAX_VALUE)
            requireRange("label_len", labelLen.toLong(), Long.MIN_VALUE, 16L)
            state.labelLen = labelLen.toLong()
            val label = reader.readBytes("label", state.labelLen)
            return DemoPacketItem(
                id = id,
                enabled = enabled,
                value = value,
                labelLen = labelLen,
                label = label
            )
        } catch (error: BinaryDecodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeDemoPacketItem(
        value: DemoPacketItem,
        writer: BinaryWriter,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "Item",
    ) {
        try {
            requireRange("id", value.id, 1L, Long.MAX_VALUE)
            requireRange("id", value.id, Long.MIN_VALUE, 999L)
            writer.writeU32("id", value.id)
            state.id = value.id
            writer.writeBool("enabled", value.enabled)
            writer.writeF64("value", value.value)
            requireRange("label_len", value.labelLen.toLong(), 1L, Long.MAX_VALUE)
            requireRange("label_len", value.labelLen.toLong(), Long.MIN_VALUE, 16L)
            requireSize("label_len.label", binarySize(value.label), value.labelLen.toLong())
            writer.writeU8("label_len", value.labelLen)
            state.labelLen = value.labelLen.toLong()
            val labelCount = state.labelLen
            requireSize("label", binarySize(value.label), labelCount)
            writer.writeBytes("label", value.label)
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeDemoPacketItem(
        writer: BinaryWriter,
        id: Long,
        enabled: Boolean,
        value: Double,
        labelLen: Int,
        label: ByteArray,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "Item",
    ) {
        try {
            requireRange("id", id, 1L, Long.MAX_VALUE)
            requireRange("id", id, Long.MIN_VALUE, 999L)
            writer.writeU32("id", id)
            state.id = id
            writer.writeBool("enabled", enabled)
            writer.writeF64("value", value)
            requireRange("label_len", labelLen.toLong(), 1L, Long.MAX_VALUE)
            requireRange("label_len", labelLen.toLong(), Long.MIN_VALUE, 16L)
            requireSize("label_len.label", binarySize(label), labelLen.toLong())
            writer.writeU8("label_len", labelLen)
            state.labelLen = labelLen.toLong()
            val labelCount = state.labelLen
            requireSize("label", binarySize(label), labelCount)
            writer.writeBytes("label", label)
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeDemoPacketItem(
        writer: BinaryWriter,
        id: Long,
        enabled: Boolean,
        value: Double,
        labelLen: Int,
        label: ByteString,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "Item",
    ) {
        try {
            requireRange("id", id, 1L, Long.MAX_VALUE)
            requireRange("id", id, Long.MIN_VALUE, 999L)
            writer.writeU32("id", id)
            state.id = id
            writer.writeBool("enabled", enabled)
            writer.writeF64("value", value)
            requireRange("label_len", labelLen.toLong(), 1L, Long.MAX_VALUE)
            requireRange("label_len", labelLen.toLong(), Long.MIN_VALUE, 16L)
            requireSize("label_len.label", binarySize(label), labelLen.toLong())
            writer.writeU8("label_len", labelLen)
            state.labelLen = labelLen.toLong()
            val labelCount = state.labelLen
            requireSize("label", binarySize(label), labelCount)
            writer.writeByteString("label", label)
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun newDemoPacketItemLabelBlock(
        bytes: ByteString,
        count: Long,
    ): EncodedBinaryBlock = EncodedBinaryBlock(
        field = "Item.label",
        count = count,
        bytes = bytes,
    )

    public fun writeDemoPacketItemLabelBlock(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "Item.label",
    ) {
        val labelCount = state.labelLen
        requireBinary(path, block.field == "Item.label", "block field ${block.field} does not match " + "Item.label")
        requireSize("$path.count", block.count, labelCount)

        requireSize("$path.bytes", block.byteSize, block.count)

        writer.writeBlock(path, block)
    }

    public fun writeDemoPacketItemLabelFragment(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: DemoPacketBinaryState = DemoPacketBinaryState(),
        path: String = "Item.label",
    ) {
        val labelCount = state.labelLen
        requireBinary(path, block.field == "Item.label", "block field ${block.field} does not match " + "Item.label")
        requireRange("$path.count", block.count, 0L, labelCount)

        requireSize("$path.bytes", block.byteSize, block.count)

        writer.writeBlock(path, block)
    }

}

public object AuditPacketKindValues {
    public const val Metric: Int = 1
    public const val Audit: Int = 2
}

public object AuditPacketFlagsValues {
    public const val HasItems: Long = 1L
    public const val Mode: Long = 6L
    public const val Reserved: Long = 4294967288L
}

public data class AuditPacket(
    public val header: AuditPacketHeader,
    public val body: AuditPacketBody
)

public fun AuditPacket.toBinaryBody(): ApiBinaryBody = AuditPacketWire.toBinaryBody(this)

public class AuditPacketBinaryState {
    public var kind: Long = 0L
    public var flags: Long = 0L
    public var itemCount: Long = 0L
    public var checksum: Long = 0L
    public var id: Long = 0L
    public var code: Long = 0L
}

public data class AuditPacketHeader(
    public val flags: Long,
    public val itemCount: Int,
    public val kind: Int = 2
)

public data class AuditPacketBody(
    public val items: List<AuditPacketItem>,
    public val checksum: Long
)

public data class AuditPacketItem(
    public val id: Long,
    public val code: Int
)

public object AuditPacketWire {
    public const val CONTENT_TYPE: String = "application/octet-stream"
    public val ENDIAN: BinaryEndian = BinaryEndian.LITTLE

    public fun body(
        contentLength: Long? = null,
        write: (BinaryWriter) -> Unit,
    ): ApiBinaryBody = StreamingBinaryBody(
        contentType = CONTENT_TYPE,
        contentLength = contentLength,
        endian = ENDIAN,
        write = write,
    )

    public fun toBinaryBody(value: AuditPacket): ApiBinaryBody = body { writer ->
        write(value, writer)
    }

    public fun parse(bytes: ByteArray): AuditPacket {
        val reader = BinaryReader(bytes, ENDIAN)
        val state = AuditPacketBinaryState()
        val header = readAuditPacketHeader(reader, state, "AuditPacket.header")
        val body = readAuditPacketBody(reader, state, "AuditPacket.body")
        reader.requireEOF("AuditPacket")
        return AuditPacket(
            header = header,
            body = body
        )
    }

    public fun write(value: AuditPacket, writer: BinaryWriter) {
        val state = AuditPacketBinaryState()
        writeAuditPacketHeader(value.header, writer, state, "AuditPacket.header")
        writeAuditPacketBody(value.body, writer, state, "AuditPacket.body")
    }

    public fun readAuditPacketHeader(
        reader: BinaryReader,
        state: AuditPacketBinaryState = AuditPacketBinaryState(),
        path: String = "AuditPacketHeader",
    ): AuditPacketHeader {
        try {
            val kind = reader.readU16("kind")
            requireBinary("kind", kind.toLong() == 2L, "const mismatch")
            state.kind = kind.toLong()
            val flags = reader.readU32("flags")
            requireRange("flags", flags, 0L, Long.MAX_VALUE)
            requireBinary("flags", ((flags) and 4294967288L) == 0L, "reserved bits must be zero")
            state.flags = flags
            val itemCount = reader.readU16("item_count")
            requireRange("item_count", itemCount.toLong(), 1L, Long.MAX_VALUE)
            requireRange("item_count", itemCount.toLong(), Long.MIN_VALUE, 4L)
            state.itemCount = itemCount.toLong()
            return AuditPacketHeader(
                flags = flags,
                itemCount = itemCount,
                kind = kind
            )
        } catch (error: BinaryDecodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeAuditPacketHeader(
        value: AuditPacketHeader,
        writer: BinaryWriter,
        state: AuditPacketBinaryState = AuditPacketBinaryState(),
        path: String = "AuditPacketHeader",
    ) {
        try {
            requireBinary("kind", value.kind.toLong() == 2L, "const mismatch")
            writer.writeU16("kind", value.kind)
            state.kind = value.kind.toLong()
            requireRange("flags", value.flags, 0L, Long.MAX_VALUE)
            requireBinary("flags", ((value.flags) and 4294967288L) == 0L, "reserved bits must be zero")
            writer.writeU32("flags", value.flags)
            state.flags = value.flags
            requireRange("item_count", value.itemCount.toLong(), 1L, Long.MAX_VALUE)
            requireRange("item_count", value.itemCount.toLong(), Long.MIN_VALUE, 4L)
            writer.writeU16("item_count", value.itemCount)
            state.itemCount = value.itemCount.toLong()
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeAuditPacketHeader(
        writer: BinaryWriter,
        flags: Long,
        itemCount: Int,
        kind: Int = 2,
        state: AuditPacketBinaryState = AuditPacketBinaryState(),
        path: String = "AuditPacketHeader",
    ) {
        try {
            requireBinary("kind", kind.toLong() == 2L, "const mismatch")
            writer.writeU16("kind", kind)
            state.kind = kind.toLong()
            requireRange("flags", flags, 0L, Long.MAX_VALUE)
            requireBinary("flags", ((flags) and 4294967288L) == 0L, "reserved bits must be zero")
            writer.writeU32("flags", flags)
            state.flags = flags
            requireRange("item_count", itemCount.toLong(), 1L, Long.MAX_VALUE)
            requireRange("item_count", itemCount.toLong(), Long.MIN_VALUE, 4L)
            writer.writeU16("item_count", itemCount)
            state.itemCount = itemCount.toLong()
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun readAuditPacketBody(
        reader: BinaryReader,
        state: AuditPacketBinaryState = AuditPacketBinaryState(),
        path: String = "AuditPacketBody",
    ): AuditPacketBody {
        try {
            val itemsCount = state.itemCount
            val items = mutableListOf<AuditPacketItem>()
            repeat(itemsCount.toInt()) { index ->
                try {
                    items += readAuditPacketItem(reader, state, "")
                } catch (error: BinaryDecodeException) {
                    throw wrapBinaryIndex("items", index, error)
                }
            }
            val checksum = reader.readU32("checksum")
            requireBinary("checksum", checksum == state.itemCount, "assert mismatch")
            state.checksum = checksum
            return AuditPacketBody(
                items = items,
                checksum = checksum
            )
        } catch (error: BinaryDecodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeAuditPacketBody(
        value: AuditPacketBody,
        writer: BinaryWriter,
        state: AuditPacketBinaryState = AuditPacketBinaryState(),
        path: String = "AuditPacketBody",
    ) {
        try {
            val itemsCount = state.itemCount
            requireSize("items", binarySize(value.items), itemsCount)
            value.items.forEachIndexed { index, item ->
                try {
                    writeAuditPacketItem(item, writer, state, "")
                } catch (error: BinaryEncodeException) {
                    throw wrapBinaryIndex("items", index, error)
                }
            }
            requireBinary("checksum", value.checksum == state.itemCount, "assert mismatch")
            writer.writeU32("checksum", value.checksum)
            state.checksum = value.checksum
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun newAuditPacketBodyItemsBlock(
        bytes: ByteString,
        count: Long,
    ): EncodedBinaryBlock = EncodedBinaryBlock(
        field = "AuditPacketBody.items",
        count = count,
        bytes = bytes,
    )

    public fun writeAuditPacketBodyItemsBlock(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: AuditPacketBinaryState = AuditPacketBinaryState(),
        path: String = "AuditPacketBody.items",
    ) {
        val itemsCount = state.itemCount
        requireBinary(path, block.field == "AuditPacketBody.items", "block field ${block.field} does not match " + "AuditPacketBody.items")
        requireSize("$path.count", block.count, itemsCount)

        writer.writeBlock(path, block)
    }

    public fun writeAuditPacketBodyItemsFragment(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: AuditPacketBinaryState = AuditPacketBinaryState(),
        path: String = "AuditPacketBody.items",
    ) {
        val itemsCount = state.itemCount
        requireBinary(path, block.field == "AuditPacketBody.items", "block field ${block.field} does not match " + "AuditPacketBody.items")
        requireRange("$path.count", block.count, 0L, itemsCount)

        writer.writeBlock(path, block)
    }

    public fun readAuditPacketItem(
        reader: BinaryReader,
        state: AuditPacketBinaryState = AuditPacketBinaryState(),
        path: String = "Item",
    ): AuditPacketItem {
        try {
            val id = reader.readU32("id")
            requireRange("id", id, 1L, Long.MAX_VALUE)
            state.id = id
            val code = reader.readU16("code")
            requireRange("code", code.toLong(), 1L, Long.MAX_VALUE)
            requireRange("code", code.toLong(), Long.MIN_VALUE, 999L)
            state.code = code.toLong()
            return AuditPacketItem(
                id = id,
                code = code
            )
        } catch (error: BinaryDecodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeAuditPacketItem(
        value: AuditPacketItem,
        writer: BinaryWriter,
        state: AuditPacketBinaryState = AuditPacketBinaryState(),
        path: String = "Item",
    ) {
        try {
            requireRange("id", value.id, 1L, Long.MAX_VALUE)
            writer.writeU32("id", value.id)
            state.id = value.id
            requireRange("code", value.code.toLong(), 1L, Long.MAX_VALUE)
            requireRange("code", value.code.toLong(), Long.MIN_VALUE, 999L)
            writer.writeU16("code", value.code)
            state.code = value.code.toLong()
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeAuditPacketItem(
        writer: BinaryWriter,
        id: Long,
        code: Int,
        state: AuditPacketBinaryState = AuditPacketBinaryState(),
        path: String = "Item",
    ) {
        try {
            requireRange("id", id, 1L, Long.MAX_VALUE)
            writer.writeU32("id", id)
            state.id = id
            requireRange("code", code.toLong(), 1L, Long.MAX_VALUE)
            requireRange("code", code.toLong(), Long.MIN_VALUE, 999L)
            writer.writeU16("code", code)
            state.code = code.toLong()
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

}

public data class WidePacket(
    public val header: WidePacketHeader,
    public val body: WidePacketBody
)

public fun WidePacket.toBinaryBody(): ApiBinaryBody = WidePacketWire.toBinaryBody(this)

public class WidePacketBinaryState {
    public var payloadLen: Long = 0L
    public var signedWide: Long = 0L
    public var marker: Long = 0L
    public var checksum: Long = 0L
}

public data class WidePacketHeader(
    public val payloadLen: Long,
    public val signedWide: Long,
    public val magic: ByteArray = byteArrayOf(87, 73, 68, 49),
    public val marker: Long = 9007199254740991L
)

public data class WidePacketBody(
    public val payload: ByteArray,
    public val checksum: Long
)

public object WidePacketWire {
    public const val CONTENT_TYPE: String = "application/octet-stream"
    public val ENDIAN: BinaryEndian = BinaryEndian.LITTLE

    public fun body(
        contentLength: Long? = null,
        write: (BinaryWriter) -> Unit,
    ): ApiBinaryBody = StreamingBinaryBody(
        contentType = CONTENT_TYPE,
        contentLength = contentLength,
        endian = ENDIAN,
        write = write,
    )

    public fun toBinaryBody(value: WidePacket): ApiBinaryBody = body { writer ->
        write(value, writer)
    }

    public fun parse(bytes: ByteArray): WidePacket {
        val reader = BinaryReader(bytes, ENDIAN)
        val state = WidePacketBinaryState()
        val header = readWidePacketHeader(reader, state, "WidePacket.header")
        val body = readWidePacketBody(reader, state, "WidePacket.body")
        reader.requireEOF("WidePacket")
        return WidePacket(
            header = header,
            body = body
        )
    }

    public fun write(value: WidePacket, writer: BinaryWriter) {
        val state = WidePacketBinaryState()
        writeWidePacketHeader(value.header, writer, state, "WidePacket.header")
        writeWidePacketBody(value.body, writer, state, "WidePacket.body")
    }

    public fun readWidePacketHeader(
        reader: BinaryReader,
        state: WidePacketBinaryState = WidePacketBinaryState(),
        path: String = "WidePacketHeader",
    ): WidePacketHeader {
        try {
            val magic = reader.readBytes("magic", 4L)
            requireBinary("magic", magic.contentEquals(byteArrayOf(87, 73, 68, 49)), "const mismatch")
            val payloadLen = reader.readU64("payload_len")
            requireRange("payload_len", payloadLen, 0L, Long.MAX_VALUE)
            requireRange("payload_len", payloadLen, Long.MIN_VALUE, 32L)
            state.payloadLen = payloadLen
            val signedWide = reader.readI64("signed_wide")
            requireRange("signed_wide", signedWide, - 5000000000L, Long.MAX_VALUE)
            requireRange("signed_wide", signedWide, Long.MIN_VALUE, 5000000000L)
            state.signedWide = signedWide
            val marker = reader.readU64("marker")
            requireBinary("marker", marker == 9007199254740991L, "const mismatch")
            state.marker = marker
            return WidePacketHeader(
                payloadLen = payloadLen,
                signedWide = signedWide,
                magic = magic,
                marker = marker
            )
        } catch (error: BinaryDecodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeWidePacketHeader(
        value: WidePacketHeader,
        writer: BinaryWriter,
        state: WidePacketBinaryState = WidePacketBinaryState(),
        path: String = "WidePacketHeader",
    ) {
        try {
            requireBinary("magic", value.magic.contentEquals(byteArrayOf(87, 73, 68, 49)), "const mismatch")
            writer.writeBytesExact("magic", value.magic, 4L)
            requireRange("payload_len", value.payloadLen, 0L, Long.MAX_VALUE)
            requireRange("payload_len", value.payloadLen, Long.MIN_VALUE, 32L)
            writer.writeU64("payload_len", value.payloadLen)
            state.payloadLen = value.payloadLen
            requireRange("signed_wide", value.signedWide, - 5000000000L, Long.MAX_VALUE)
            requireRange("signed_wide", value.signedWide, Long.MIN_VALUE, 5000000000L)
            writer.writeI64("signed_wide", value.signedWide)
            state.signedWide = value.signedWide
            requireBinary("marker", value.marker == 9007199254740991L, "const mismatch")
            writer.writeU64("marker", value.marker)
            state.marker = value.marker
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeWidePacketHeader(
        writer: BinaryWriter,
        payloadLen: Long,
        signedWide: Long,
        magic: ByteArray = byteArrayOf(87, 73, 68, 49),
        marker: Long = 9007199254740991L,
        state: WidePacketBinaryState = WidePacketBinaryState(),
        path: String = "WidePacketHeader",
    ) {
        try {
            requireBinary("magic", magic.contentEquals(byteArrayOf(87, 73, 68, 49)), "const mismatch")
            writer.writeBytesExact("magic", magic, 4L)
            requireRange("payload_len", payloadLen, 0L, Long.MAX_VALUE)
            requireRange("payload_len", payloadLen, Long.MIN_VALUE, 32L)
            writer.writeU64("payload_len", payloadLen)
            state.payloadLen = payloadLen
            requireRange("signed_wide", signedWide, - 5000000000L, Long.MAX_VALUE)
            requireRange("signed_wide", signedWide, Long.MIN_VALUE, 5000000000L)
            writer.writeI64("signed_wide", signedWide)
            state.signedWide = signedWide
            requireBinary("marker", marker == 9007199254740991L, "const mismatch")
            writer.writeU64("marker", marker)
            state.marker = marker
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun readWidePacketBody(
        reader: BinaryReader,
        state: WidePacketBinaryState = WidePacketBinaryState(),
        path: String = "WidePacketBody",
    ): WidePacketBody {
        try {
            val payload = reader.readBytes("payload", state.payloadLen)
            val checksum = reader.readU64("checksum")
            requireBinary("checksum", checksum == state.payloadLen, "assert mismatch")
            state.checksum = checksum
            return WidePacketBody(
                payload = payload,
                checksum = checksum
            )
        } catch (error: BinaryDecodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeWidePacketBody(
        value: WidePacketBody,
        writer: BinaryWriter,
        state: WidePacketBinaryState = WidePacketBinaryState(),
        path: String = "WidePacketBody",
    ) {
        try {
            val payloadCount = state.payloadLen
            requireSize("payload", binarySize(value.payload), payloadCount)
            writer.writeBytes("payload", value.payload)
            requireBinary("checksum", value.checksum == state.payloadLen, "assert mismatch")
            writer.writeU64("checksum", value.checksum)
            state.checksum = value.checksum
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeWidePacketBody(
        writer: BinaryWriter,
        payload: ByteArray,
        checksum: Long,
        state: WidePacketBinaryState = WidePacketBinaryState(),
        path: String = "WidePacketBody",
    ) {
        try {
            val payloadCount = state.payloadLen
            requireSize("payload", binarySize(payload), payloadCount)
            writer.writeBytes("payload", payload)
            requireBinary("checksum", checksum == state.payloadLen, "assert mismatch")
            writer.writeU64("checksum", checksum)
            state.checksum = checksum
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun writeWidePacketBody(
        writer: BinaryWriter,
        payload: ByteString,
        checksum: Long,
        state: WidePacketBinaryState = WidePacketBinaryState(),
        path: String = "WidePacketBody",
    ) {
        try {
            val payloadCount = state.payloadLen
            requireSize("payload", binarySize(payload), payloadCount)
            writer.writeByteString("payload", payload)
            requireBinary("checksum", checksum == state.payloadLen, "assert mismatch")
            writer.writeU64("checksum", checksum)
            state.checksum = checksum
        } catch (error: BinaryEncodeException) {
            throw wrapBinaryField(path, error)
        }
    }

    public fun newWidePacketBodyPayloadBlock(
        bytes: ByteString,
        count: Long,
    ): EncodedBinaryBlock = EncodedBinaryBlock(
        field = "WidePacketBody.payload",
        count = count,
        bytes = bytes,
    )

    public fun writeWidePacketBodyPayloadBlock(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: WidePacketBinaryState = WidePacketBinaryState(),
        path: String = "WidePacketBody.payload",
    ) {
        val payloadCount = state.payloadLen
        requireBinary(path, block.field == "WidePacketBody.payload", "block field ${block.field} does not match " + "WidePacketBody.payload")
        requireSize("$path.count", block.count, payloadCount)

        requireSize("$path.bytes", block.byteSize, block.count)

        writer.writeBlock(path, block)
    }

    public fun writeWidePacketBodyPayloadFragment(
        writer: BinaryWriter,
        block: EncodedBinaryBlock,
        state: WidePacketBinaryState = WidePacketBinaryState(),
        path: String = "WidePacketBody.payload",
    ) {
        val payloadCount = state.payloadLen
        requireBinary(path, block.field == "WidePacketBody.payload", "block field ${block.field} does not match " + "WidePacketBody.payload")
        requireRange("$path.count", block.count, 0L, payloadCount)

        requireSize("$path.bytes", block.byteSize, block.count)

        writer.writeBlock(path, block)
    }

}
