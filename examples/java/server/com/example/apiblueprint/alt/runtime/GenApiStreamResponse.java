// Code generated by api-blueprint (Java server); DO NOT EDIT.
package com.example.apiblueprint.alt.runtime;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.io.InputStream;
import java.util.Map;

public record GenApiStreamResponse(
    InputStream body,
    String contentType,
    Map<String, String> headers
) implements AutoCloseable {
    public GenApiStreamResponse {
        body = body == null ? InputStream.nullInputStream() : body;
        contentType = contentType == null || contentType.isBlank() ? "application/octet-stream" : contentType;
        headers = headers == null ? Map.of() : Map.copyOf(headers);
    }

    public byte[] readAllBytes() throws IOException {
        return body.readAllBytes();
    }

    @Override
    public void close() throws IOException {
        body.close();
    }

    public static GenApiStreamResponse of(byte[] body, String contentType) {
        byte[] bytes = body == null ? new byte[0] : body.clone();
        return new GenApiStreamResponse(new ByteArrayInputStream(bytes), contentType, Map.of());
    }
}
