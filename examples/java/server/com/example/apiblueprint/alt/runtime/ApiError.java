// Code generated by api-blueprint (Java server); DO NOT EDIT.
package com.example.apiblueprint.alt.runtime;

public class ApiError extends RuntimeException {
    private final ApiErrorPayload payload;
    private final String routeId;
    private final String rawBody;
    private final ApiErrorEntry entry;

    public ApiError(ApiErrorPayload payload, String routeId, String rawBody, ApiErrorEntry entry) {
        super(payload.message() == null || payload.message().isBlank() ? "API error " + payload.code() : payload.message());
        this.payload = payload;
        this.routeId = routeId;
        this.rawBody = rawBody;
        this.entry = entry;
    }

    public ApiErrorPayload payload() {
        return payload;
    }

    public int code() {
        return payload.code();
    }

    public String id() {
        return payload.id();
    }

    public String group() {
        return payload.group();
    }

    public String key() {
        return payload.key();
    }

    public String apiMessage() {
        return payload.message();
    }

    public ApiToastPayload toast() {
        return payload.toast();
    }

    public String routeId() {
        return routeId;
    }

    public String rawBody() {
        return rawBody;
    }

    public ApiErrorEntry entry() {
        return entry;
    }

    public boolean is(ApiErrorEntry expected) {
        if (expected == null) {
            return false;
        }
        if (expected.id() != null && !expected.id().isBlank() && expected.id().equals(id())) {
            return true;
        }
        return expected.code() != 0 && expected.code() == code();
    }

    public static String resolveApiToast(ApiToastPayload toast, java.util.function.Function<String, String> translate, String fallbackMessage) {
        if (toast == null) {
            return fallbackMessage;
        }
        if (!toast.text().isEmpty()) {
            return toast.text();
        }
        if (!toast.key().isEmpty() && translate != null) {
            String translated = translate.apply(toast.key());
            if (translated != null && !translated.isEmpty()) {
                return translated;
            }
        }
        return toast.defaultMessage().isEmpty() ? fallbackMessage : toast.defaultMessage();
    }
}