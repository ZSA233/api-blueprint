# Code generated by api-blueprint (Python server); DO NOT EDIT.
from __future__ import annotations

from dataclasses import dataclass
from enum import Enum, IntEnum, StrEnum
from typing import Any, Callable, Generic, Mapping, Self, TypeVar
from ....runtime.server import ApiUploadFile
from ....runtime.gen_codecs import (
    _MISSING,
    _api_to_json,
    _api_to_transport,
    _decode_any,
    _decode_bool,
    _decode_bytes,
    _decode_file,
    _decode_float,
    _decode_int,
    _decode_list,
    _decode_map,
    _decode_object,
    _decode_optional,
    _decode_required,
    _decode_str,
    _field_path,
)


R = TypeVar("R")


@dataclass(kw_only=True)
class RoomListResponse:
    rooms: list[RoomSummary]

    @classmethod
    def from_mapping(cls, value: Mapping[str, Any]) -> Self:
        if not isinstance(value, Mapping):
            raise TypeError("RoomListResponse: expected object")
        return cls._from_mapping(value, "RoomListResponse")

    @classmethod
    def from_value(cls, value: object, path: str = "RoomListResponse") -> Self:
        if isinstance(value, cls):
            return value
        if not isinstance(value, Mapping):
            raise TypeError(f"{path}: expected object")
        return cls._from_mapping(value, path)

    @classmethod
    def _from_mapping(cls, value: Mapping[str, Any], path: str) -> Self:
        return cls(
            rooms=_decode_required(lambda item, path: _decode_list(item, path, RoomSummary.from_value), value.get("rooms", _MISSING), _field_path(path, "rooms")),
        )

    def to_mapping(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        result["rooms"] = _api_to_json(self.rooms)
        return result

    def to_transport_mapping(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        result["rooms"] = _api_to_transport(self.rooms)
        return result


@dataclass(kw_only=True)
class RoomSummary:
    room_id: str
    title: str

    @classmethod
    def from_mapping(cls, value: Mapping[str, Any]) -> Self:
        if not isinstance(value, Mapping):
            raise TypeError("RoomSummary: expected object")
        return cls._from_mapping(value, "RoomSummary")

    @classmethod
    def from_value(cls, value: object, path: str = "RoomSummary") -> Self:
        if isinstance(value, cls):
            return value
        if not isinstance(value, Mapping):
            raise TypeError(f"{path}: expected object")
        return cls._from_mapping(value, path)

    @classmethod
    def _from_mapping(cls, value: Mapping[str, Any], path: str) -> Self:
        return cls(
            room_id=_decode_required(_decode_str, value.get("room_id", _MISSING), _field_path(path, "room_id")),
            title=_decode_required(_decode_str, value.get("title", _MISSING), _field_path(path, "title")),
        )

    def to_mapping(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        result["room_id"] = _api_to_json(self.room_id)
        result["title"] = _api_to_json(self.title)
        return result

    def to_transport_mapping(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        result["room_id"] = _api_to_transport(self.room_id)
        result["title"] = _api_to_transport(self.title)
        return result
