# Code generated by api-blueprint (Python server); DO NOT EDIT.
from __future__ import annotations

from dataclasses import dataclass
from enum import Enum, IntEnum, StrEnum
from typing import Any, Callable, Generic, Mapping, Protocol, Self, TypeVar
from ....runtime.server import ApiUploadFile
from ....runtime.gen_codecs import (
    _MISSING,
    _api_to_json,
    _api_to_transport,
    _decode_any,
    _decode_bool,
    _decode_bytes,
    _decode_coerce_string,
    _decode_file,
    _decode_float,
    _decode_int,
    _decode_list,
    _decode_map,
    _decode_object,
    _decode_one_of,
    _decode_optional,
    _decode_required,
    _decode_str,
    _decode_strict_bool,
    _decode_strict_float,
    _decode_strict_int,
    _field_path,
)


R = TypeVar("R")
C = TypeVar("C")


@dataclass(kw_only=True)
class MediaPreviewForm:
    title: str | None = None
    image: ApiUploadFile

    @classmethod
    def from_mapping(cls, value: Mapping[str, Any]) -> Self:
        if not isinstance(value, Mapping):
            raise TypeError("MediaPreviewForm: expected object")
        return cls._from_mapping(value, "MediaPreviewForm")

    @classmethod
    def from_value(cls, value: object, path: str = "MediaPreviewForm") -> Self:
        if isinstance(value, cls):
            return value
        if not isinstance(value, Mapping):
            raise TypeError(f"{path}: expected object")
        return cls._from_mapping(value, path)

    @classmethod
    def _from_mapping(cls, value: Mapping[str, Any], path: str) -> Self:
        return cls(
            title=_decode_optional(_decode_str, value.get("title", _MISSING), _field_path(path, "title")),
            image=_decode_required(_decode_file, value.get("image", _MISSING), _field_path(path, "image")),
        )

    def to_mapping(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        if self.title is not None:
            result["title"] = _api_to_json(self.title)

        result["image"] = _api_to_json(self.image)
        return result

    def to_transport_mapping(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        if self.title is not None:
            result["title"] = _api_to_transport(self.title)

        result["image"] = _api_to_transport(self.image)
        return result


@dataclass(kw_only=True)
class MediaErrorFrameQuery:
    mode: str | None = None

    @classmethod
    def from_mapping(cls, value: Mapping[str, Any]) -> Self:
        if not isinstance(value, Mapping):
            raise TypeError("MediaErrorFrameQuery: expected object")
        return cls._from_mapping(value, "MediaErrorFrameQuery")

    @classmethod
    def from_value(cls, value: object, path: str = "MediaErrorFrameQuery") -> Self:
        if isinstance(value, cls):
            return value
        if not isinstance(value, Mapping):
            raise TypeError(f"{path}: expected object")
        return cls._from_mapping(value, path)

    @classmethod
    def _from_mapping(cls, value: Mapping[str, Any], path: str) -> Self:
        return cls(
            mode=_decode_optional(_decode_str, value.get("mode", _MISSING), _field_path(path, "mode")),
        )

    def to_mapping(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        if self.mode is not None:
            result["mode"] = _api_to_json(self.mode)

        return result

    def to_transport_mapping(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        if self.mode is not None:
            result["mode"] = _api_to_transport(self.mode)

        return result
