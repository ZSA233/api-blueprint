/* eslint-disable */
/* istanbul ignore file */
// Code generated by api-blueprint (TypeScript client); DO NOT EDIT.
export {};

import type {
  AnonFunc1putAnonKv,
  ApiDemoA,
  ApiDemoMap,
  AssistantCancel,
  AssistantDelta,
  AssistantDone,
  AssistantInput,
  SweepLog,
  SweepProgress,
  SweepState,
} from "../../../runtime/types";

// Route Contracts

export interface AbcQuery {
  /** arg1 */
  arg1?: boolean;
  /** arg3 */
  arg3?: string;
  /** arg2 */
  arg2?: number;
}

export type AbcResponse = ApiDemoA;

export type AssistantClientMessage =
  | { type: "input"; data: AssistantInput }
  | { type: "cancel"; data: AssistantCancel };

export type AssistantServerMessage =
  | { type: "delta"; data: AssistantDelta }
  | { type: "done"; data: AssistantDone }
  | { type: "log"; data: SweepLog };

export type AssistantSessionResponse = void;

export interface DeleteQuery {
  /** arg1 */
  arg1?: string;
  /** arg2 */
  arg2?: number;
}

export type DeleteResponse = string;

export interface ErrorDemoQuery {
  /** ok/token/rate_limit/unknown */
  mode?: string;
}

export interface ErrorDemoResponse {
  /** status */
  status: string;
}

export interface FormSubmitForm {
  /** title */
  title: string;
  /** count */
  count?: number;
  /** enabled */
  enabled?: boolean;
}

export interface FormSubmitResponse {
  /** summary */
  summary: string;
  /** count */
  count: number;
  /** enabled */
  enabled: boolean;
}

export type MapModelResponse = Record<number, ApiDemoMap>;

export interface PostDeprecatedJSON {
  /** req1 */
  req1: string;
  /** req2 */
  req2?: number;
}

export interface PostDeprecatedResponse {
  /** list */
  list: Array<string>;
}

export interface PutDemoJSON {
  /** req1 */
  req1: string;
  /** req2 */
  req2?: number;
}

export interface PutDemoQuery {
  /** arg1 */
  arg1?: string;
  /** arg2 */
  arg2?: number;
  /** arg3 */
  arg3?: string;
}

export interface PutDemoResponse {
  /** list */
  list: Array<string>;

  anon_kv: AnonFunc1putAnonKv;
}

export interface RawResponse {
  /** list */
  list: Array<string>;
  /** list2 */
  list2: Record<number, Array<ApiDemoA>>;
}

export type SweepEventsResponse = void;

export type SweepStreamMessage =
  | { type: "state"; data: SweepState }
  | { type: "progress"; data: SweepProgress }
  | { type: "log"; data: SweepLog };

export interface TestPostJSON {
  /** req1 */
  req1: string;
  /** req2 */
  req2?: number;
}

export interface TestPostResponse {
  /** list */
  list: Array<string>;
  /** [str: ApiDemoMap] map */
  map: Record<string, ApiDemoMap>;
}

export const SweepStreamMessageVariants = {
  state(data: SweepState): SweepStreamMessage {
    return { type: "state", data };
  },
  progress(data: SweepProgress): SweepStreamMessage {
    return { type: "progress", data };
  },
  log(data: SweepLog): SweepStreamMessage {
    return { type: "log", data };
  },
} as const;

export type SweepStreamMessageHandlers<R> = {
  state: (data: SweepState, message: Extract<SweepStreamMessage, { type: "state" }>) => R;
  progress: (data: SweepProgress, message: Extract<SweepStreamMessage, { type: "progress" }>) => R;
  log: (data: SweepLog, message: Extract<SweepStreamMessage, { type: "log" }>) => R;
};

export type SweepStreamMessageDispatchErrorKind = "unknown_type";

export class SweepStreamMessageDispatchError extends Error {
  readonly kind: SweepStreamMessageDispatchErrorKind;
  readonly type: string;
  readonly rawMessage: SweepStreamMessage;

  constructor(kind: SweepStreamMessageDispatchErrorKind, rawMessage: SweepStreamMessage) {
    const type = String((rawMessage as { type?: unknown }).type);
    super(`unsupported SweepStreamMessage type ${type}`);
    this.name = "SweepStreamMessageDispatchError";
    this.kind = kind;
    this.type = type;
    this.rawMessage = rawMessage;
    Object.setPrototypeOf(this, SweepStreamMessageDispatchError.prototype);
  }
}

export function isSweepStreamMessageDispatchError(error: unknown): error is SweepStreamMessageDispatchError {
  return error instanceof SweepStreamMessageDispatchError;
}

export function dispatchSweepStreamMessage<R>(
  message: SweepStreamMessage,
  handlers: SweepStreamMessageHandlers<R>,
): R {
  switch (message.type) {
    case "state": return handlers.state(message.data, message);
    case "progress": return handlers.progress(message.data, message);
    case "log": return handlers.log(message.data, message);
    default:
      throw new SweepStreamMessageDispatchError("unknown_type", message);
  }
}

export const AssistantServerMessageVariants = {
  delta(data: AssistantDelta): AssistantServerMessage {
    return { type: "delta", data };
  },
  done(data: AssistantDone): AssistantServerMessage {
    return { type: "done", data };
  },
  log(data: SweepLog): AssistantServerMessage {
    return { type: "log", data };
  },
} as const;

export type AssistantServerMessageHandlers<R> = {
  delta: (data: AssistantDelta, message: Extract<AssistantServerMessage, { type: "delta" }>) => R;
  done: (data: AssistantDone, message: Extract<AssistantServerMessage, { type: "done" }>) => R;
  log: (data: SweepLog, message: Extract<AssistantServerMessage, { type: "log" }>) => R;
};

export type AssistantServerMessageDispatchErrorKind = "unknown_type";

export class AssistantServerMessageDispatchError extends Error {
  readonly kind: AssistantServerMessageDispatchErrorKind;
  readonly type: string;
  readonly rawMessage: AssistantServerMessage;

  constructor(kind: AssistantServerMessageDispatchErrorKind, rawMessage: AssistantServerMessage) {
    const type = String((rawMessage as { type?: unknown }).type);
    super(`unsupported AssistantServerMessage type ${type}`);
    this.name = "AssistantServerMessageDispatchError";
    this.kind = kind;
    this.type = type;
    this.rawMessage = rawMessage;
    Object.setPrototypeOf(this, AssistantServerMessageDispatchError.prototype);
  }
}

export function isAssistantServerMessageDispatchError(error: unknown): error is AssistantServerMessageDispatchError {
  return error instanceof AssistantServerMessageDispatchError;
}

export function dispatchAssistantServerMessage<R>(
  message: AssistantServerMessage,
  handlers: AssistantServerMessageHandlers<R>,
): R {
  switch (message.type) {
    case "delta": return handlers.delta(message.data, message);
    case "done": return handlers.done(message.data, message);
    case "log": return handlers.log(message.data, message);
    default:
      throw new AssistantServerMessageDispatchError("unknown_type", message);
  }
}

export const AssistantClientMessageVariants = {
  input(data: AssistantInput): AssistantClientMessage {
    return { type: "input", data };
  },
  cancel(data: AssistantCancel): AssistantClientMessage {
    return { type: "cancel", data };
  },
} as const;

export type AssistantClientMessageHandlers<R> = {
  input: (data: AssistantInput, message: Extract<AssistantClientMessage, { type: "input" }>) => R;
  cancel: (data: AssistantCancel, message: Extract<AssistantClientMessage, { type: "cancel" }>) => R;
};

export type AssistantClientMessageDispatchErrorKind = "unknown_type";

export class AssistantClientMessageDispatchError extends Error {
  readonly kind: AssistantClientMessageDispatchErrorKind;
  readonly type: string;
  readonly rawMessage: AssistantClientMessage;

  constructor(kind: AssistantClientMessageDispatchErrorKind, rawMessage: AssistantClientMessage) {
    const type = String((rawMessage as { type?: unknown }).type);
    super(`unsupported AssistantClientMessage type ${type}`);
    this.name = "AssistantClientMessageDispatchError";
    this.kind = kind;
    this.type = type;
    this.rawMessage = rawMessage;
    Object.setPrototypeOf(this, AssistantClientMessageDispatchError.prototype);
  }
}

export function isAssistantClientMessageDispatchError(error: unknown): error is AssistantClientMessageDispatchError {
  return error instanceof AssistantClientMessageDispatchError;
}

export function dispatchAssistantClientMessage<R>(
  message: AssistantClientMessage,
  handlers: AssistantClientMessageHandlers<R>,
): R {
  switch (message.type) {
    case "input": return handlers.input(message.data, message);
    case "cancel": return handlers.cancel(message.data, message);
    default:
      throw new AssistantClientMessageDispatchError("unknown_type", message);
  }
}

